(executable
 (name main)
 (modules main)
 (libraries
  core
  core_unix
  core_unix.sys_unix
  core_unix.command_unix
  core_unix.filename_unix
  unix
  rounds_lib)
 (preprocess (pps ppx_jane)))

(executable
 (name tui)
 (modules tui)
 (libraries core core_unix.command_unix rounds_tui_lib)
 (preprocess (pps ppx_jane)))
